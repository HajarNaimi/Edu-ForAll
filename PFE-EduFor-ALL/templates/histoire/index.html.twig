{% extends "base.html.twig" %}
{% block title %}
<a href={{path("liste")}} 
class="btn btn-danger  d-none d-md-block pull-right ms-3 hidden-xs hidden-sm waves-effect waves-light text-white">
Gestion des Histoires</a>
<style>.inputSelect110 {
    width: 735px;
    height: 40px;
    border-radius: 2px;
    border: 2px solid #e6e6e6;
    border-radius: 5px;
      /* content: "\f00c"; */
      font-family: FontAwesome;
      font-size: 15px;
      color: #333333;
  }
  </style>
{% endblock %}
            {% block body %}
           {# {{ form_start(form) }}
            <div class="wrap-input100 validate-input m-b-26" data-validate="Nom is required">
            <span class="label-input100">Nom</span>
            {{ form_widget(form.nom, { 'attr': {'class': 'input100'} }) }}
        </div>
            {{ form_end(form) }}#}
            <div class="container-fluid">
                <!-- ============================================================== -->
                <!-- Start Page Content -->
                <!-- ============================================================== -->
                <div class="row">
                    <div class="col-sm-12">
                        <div class="white-box">
                            <h3 class="box-title">Ajouter un Histoire</h3>
                            <p class="text-muted"></p>
                            <div class="table-responsive">
                            
                            {{ form_start(form , { 'attr': {'class': 'login100-form validate-form'} }) }}
					<div class="wrap-input100 validate-input m-b-26" data-validate="Nom is required">
						<span class="label-input100">Titre</span>
						{{ form_widget(form.titre, { 'attr': {'class': 'input100'} }) }}
					</div>

                    <div class="wrap-input100 validate-input m-b-26" data-validate="Prenom is required">
						<span class="label-input100">Histoire</span>
                        {{ form_widget(form.histoire, { 'attr': {'class': 'input100'} }) }}
					</div>


			

                   

					

				

				
                   

					

					<div class="container-login100-form-btn">
                    {{ form_widget(form.save, { 'attr': {'class': 'login100-form-btn'} }) }}
						
					</div>
				</form>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- ============================================================== -->
                <!-- End PAge Content -->
                <!-- ============================================================== -->
                <!-- ============================================================== -->
                <!-- Right sidebar -->
                <!-- ============================================================== -->
                <!-- .right-sidebar -->
                <!-- ============================================================== -->
                <!-- End Right sidebar -->
                <!-- ============================================================== -->
            </div>
            <!-- ============================================================== -->
            <!-- End Container fluid  -->
            <!-- ============================================================== -->
            <!-- ============================================================== -->
            <!-- footer -->
            <!-- ============================================================== -->
            {% endblock %}